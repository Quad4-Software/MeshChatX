// SPDX-License-Identifier: 0BSD

/** Default probe payload size in bytes */
export const DEFAULT_PROBE_SIZE = 16;

/** Minimum probe payload size in bytes */
export const MIN_PROBE_SIZE = 1;

/** Maximum probe payload size in bytes */
export const MAX_PROBE_SIZE = 1024;

/** Default number of probe packets */
export const DEFAULT_PROBE_COUNT = 1;

/** Minimum number of probe packets */
export const MIN_PROBES = 1;

/** Maximum number of probe packets */
export const MAX_PROBES = 50;

/** Default wait between probes in seconds */
export const DEFAULT_PROBE_WAIT = 0;

/** Default full destination aspect name */
export const DEFAULT_PROBE_FULL_NAME = "lxmf.delivery";

/** API endpoint for rnprobe */
export const RNPROBE_API_ENDPOINT = "/api/v1/rnprobe";
